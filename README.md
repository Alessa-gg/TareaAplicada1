# TareaAplicada1

## Promts
###1 Crea una excepción personalizada llamada TareaNoEncontradaException que herede de Exception y muestra cómo lanzarla cuando un ID ingresado no coincida con ningún elemento de la lista.

###2 Implementa una validación con DateTime.TryParse en C# para asegurar que el usuario ingrese una fecha de vencimiento correcta (dd/mm/aaaa) y lanza una excepción si el formato no es válido.
###3 Escribe la lógica para el método CargarTareas en C# de modo que valide primero si el archivo tareas.txt realmente existe en el disco usando File.Exists antes de intentar abrirlo con StreamReader. Si el archivo no existe, debe crear uno nuevo vacío de forma automática para evitar que el programa falle en la primera ejecución.
